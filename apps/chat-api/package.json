{
  "name": "@exactly/chat-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "tsc",
    "build:bun": "bun build src/index.ts --outdir dist --target bun",
    "start": "node dist/index.js",
    "start:bun": "bun run dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.0.0",
    "@exactly/agents": "workspace:*",
    "@exactly/auth": "workspace:*",
    "@exactly/db": "workspace:*",
    "@exactly/events": "workspace:*",
    "@exactly/types": "workspace:*",
    "@hono/node-server": "^1.19.9",
    "ai": "^4.0.0",
    "drizzle-orm": "^0.38.3",
    "hono": "^4.6.14",
    "postgres": "^3.4.5",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@types/node": "^25.0.9",
    "typescript": "^5.7.2"
  }
}
