{
  "name": "@exactly/workflows-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "tsc",
    "build:bun": "bun build src/index.ts --outdir dist --target bun",
    "start": "node dist/index.js",
    "start:bun": "bun run dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@exactly/db": "workspace:*",
    "@exactly/events": "workspace:*",
    "@exactly/types": "workspace:*",
    "@hono/node-server": "^1.19.9",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
